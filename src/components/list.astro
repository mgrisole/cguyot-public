---
interface Props {
  items: string[];
  darkFont?: boolean;
}

const { items, darkFont } = Astro.props;
---

<ul class="gap-3 pl-3 flex flex-col">
  {
    items.map((item) => (
      <li class="flex-row gap-3 items-center flex">
        <div>{item}</div>
      </li>
    ))
  }
</ul>
